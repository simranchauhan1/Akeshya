<!-- src/components/Services.astro -->

---
import Heading from '../components/Heading.astro';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faPalette, faCode, faBullhorn, faLifeRing } from '@fortawesome/free-solid-svg-icons';

const title = 'SERVICES';
const description = 'Akeshya will serve as your consultant and development partner to help you turn your idea into a reality.';
const services = [
    { title: 'Design', description: "Our web design services can assist you in reclaiming your company's online image. Your business will flourish on the Internet thanks to the combination of style and technology we provide, as well as our experience.", icon: faPalette },
    { title: 'Development', description: "Our development team can construct platforms to help your business thrive by creating powerful customised solutions tailored to your particular requirements. Akeshya makes use of established and effective web development tools.", icon: faCode },
    { title: 'Marketing', description: "A beautiful website is the foundation of effective marketing. Our customers achieve success where it counts—in the real world—by combining our proven approach with our passion for improving conversions and increasing ROI.", icon: faBullhorn },
    { title: 'Support', description: "Since the beginning, we at Akeshya have specialised in website maintenance. We recognise the significance of having your business online 24 hours a day, seven days a week, and we've created a system to ensure that we're always available.", icon: faLifeRing }
];
---

<section class="services-section" id="service">
    <h2><Heading /></h2>
    <p class="services-description">{description}</p>
    <div class="services-container">
        {services.map((service, index) => (
            <div class="service-box" data-index={index}>
                <FontAwesomeIcon icon={service.icon} className="service-icon" />
                <h3>{service.title}</h3>
                <p>{service.description}</p>
            </div>
        ))}
    </div>

    <!-- CSS Styles -->

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Raleway:wght@400;700&display=swap');

        .services-section {
            background-color: #f9f9f9;
            padding: 40px 20px;
            text-align: center;
            font-size: 1.5rem;
        }

        .services-description {
            font-size: 1.1rem;
            color: #666;
            margin-bottom: 30px;
        }

        .services-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
        }

        .service-box {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            flex: 1 1 calc(25% - 20px); /* Responsive layout with gap */
            min-width: 200px; /* Ensure minimum width */
            text-align: left;
            max-width: 250px;
            opacity: 0; /* Initial opacity for animation */
            transform: translateY(50px); /* Initial position for animation */
            transition: opacity 0.3s ease, transform 0.3s ease;
        }

        .service-box.visible {
            opacity: 1;
            transition: opacity 0.3 ease;
            transform: translateY(0); /* Final position */
        }

        .service-box:hover {
            background-color: #1f19d4; /* Blue color */
            color: #fff; /* White text */
        }

        .service-box:hover h3,
        .service-box:hover p {
            color: #fff; /* White text */
        }

        .service-box h3 {
            font-size: 1.5rem;
            color: #0e0d0d;
            margin: 0 0 10px;
            font-family: 'Raleway', sans-serif;
        }

        .service-box p {
            font-size: 1rem;
            color: #666;
            line-height: 1.5;
            font-family: 'Open Sans', sans-serif;
        }

        .service-icon {
            height: 1.5rem;
            font-size: 2rem;
            color: #1f19d4;
            margin-bottom: 10px;
            border: 10px solid #1f19d4; /* Thick border */
            border-radius: 50%; /* Circular border */
            padding: 3px;
        }

        .service-box:hover .service-icon {
            color: #fff; /* White icon on hover */
            border: 10px solid #fff;
        }

        /* Animation delay */
        .service-box[data-index="0"] {
            transition-delay: 0.1s;
        }

        .service-box[data-index="1"] {
            transition-delay: 0.3s;
        }

        .service-box[data-index="2"] {
            transition-delay: 0.5s;
        }

        .service-box[data-index="3"] {
            transition-delay: 0.7s;
        }
    </style>
</section>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const observer = new IntersectionObserver((entries, observer) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                    observer.unobserve(entry.target);
                }
            });
        }, { threshold: 0.1 });

        document.querySelectorAll('.service-box').forEach(box => {
            observer.observe(box);
        });
    });
</script>
